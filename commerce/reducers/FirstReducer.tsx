const initialState = {
    loading:false,
    error: null,
    current:null,
}

const first= (state=initialState,action:any) => {
    

    switch (action.type) {


        default:
            return state
    }
}

export default first